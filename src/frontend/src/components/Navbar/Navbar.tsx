import MainNav from "./MainNav";
import CategoryBar from "./CategoryBar";

const Navbar = () => {
  return (
    <nav>
      <MainNav />
      <CategoryBar />
    </nav>
  );
};

export default Navbar;
